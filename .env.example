# Optional dev fallback. The normal way to configure a model is the in-app
# Settings page (keys are stored in the local, gitignored SQLite database).
# If set, this key is used for Gemini when no provider is configured in-app.
GEMINI_API_KEY=
